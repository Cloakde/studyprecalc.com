# Source Code

Application source code belongs here.

The current implementation is a React + Vite + TypeScript web app. Desktop-specific code should stay isolated in `src/desktop/` when the desktop port begins.
