# Tests

Verification files belong here.

- `unit/` - focused tests for domain and utility behavior.
- `integration/` - tests for larger app flows.
- `fixtures/` - neutral test-only questions, attempts, and media metadata.
