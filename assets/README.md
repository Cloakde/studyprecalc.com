# Assets

Static app assets belong here.

- `icons/` - application icons and UI icons.
- `images/` - images used by the app shell or UI.
- `videos/` - local video assets if the app later supports bundled video explanations.

Large videos may eventually belong outside Git with references stored in content metadata.
